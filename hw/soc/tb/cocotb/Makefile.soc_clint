# SPDX-FileCopyrightText: 2026 Hasan Melih Akbulut
# SPDX-License-Identifier: Apache-2.0

# cocotb + Icarus suite for hw/soc/rtl/soc_clint.v.
#
#   cd hw/soc/tb/cocotb && make -f Makefile.soc_clint        run the suite
#   cd hw/soc/tb/cocotb && make -f Makefile.soc_clint lint   Verilator lint
#
# The expectations in test_soc_clint.py come from the RISC-V privileged
# specification's machine timer, the standard CLINT register layout, the
# fabric slave contract in hw/soc/rtl/soc_bus.v, and the generated memory
# map sw/golden/memmap_gen.py -- not from the RTL, so this suite is meant
# to keep failing usefully when regmap/memmap.yaml changes.
#
# Standalone by design, same structure as hw/tb/Makefile.regbank. Same
# rootless tool discovery as that file: iverilog from PATH, cocotb from the
# project venv hw/.venv -- if cocotb-config is not already on PATH the
# recipe re-invokes make with the venv prepended, because make < 4.4 does
# not pass an exported PATH into $(shell ...), which the cocotb makefiles
# rely on. The venv is three directories up from here (hw/.venv), not in
# hw/soc.

SIM ?= icarus
TOPLEVEL_LANG ?= verilog

THIS_MK := $(lastword $(MAKEFILE_LIST))

VENV_BIN := $(abspath $(dir $(THIS_MK))/../../../.venv/bin)

ifeq ($(shell command -v cocotb-config 2>/dev/null),)

GOALS := $(if $(MAKECMDGOALS),$(MAKECMDGOALS),sim)
.PHONY: $(GOALS)
$(GOALS):
	@PATH="$(VENV_BIN):$$PATH" $(MAKE) -f $(THIS_MK) --no-print-directory $@

else

RTL = $(PWD)/../../rtl

# SOC_CLINT_SRC exists so a mutation run can point the same suite at a
# modified copy of the RTL without editing the tree
# (make -f Makefile.soc_clint SOC_CLINT_SRC=/path/to/mutant.v SIM_BUILD=... ).
SOC_CLINT_SRC ?= $(RTL)/soc_clint.v

# hw/rtl/secded_enc.v and hw/rtl/secded_dec.v are READ from the pilot's
# directory and never modified -- docs/34 pins hw/rtl by git blob hash --
# exactly as Makefile.ibex_regfile_secded already reads them and as
# Makefile.soc_wdog reads tmr_voter.v. soc_clint.v instantiates them at
# HARDEN = 1 (docs/58 H6) and does not at HARDEN = 0; they are listed
# unconditionally, because a source list that moves with a parameter is
# a source list two measurements cannot share.
PILOT_RTL = $(PWD)/../../../rtl

VERILOG_SOURCES = $(SOC_CLINT_SRC) \
                  $(PILOT_RTL)/secded_enc.v $(PILOT_RTL)/secded_dec.v
TOPLEVEL = soc_clint
MODULE = test_soc_clint

# Per-block build and results paths so this suite can run alongside the
# others without clobbering their artifacts.
SIM_BUILD ?= sim_build_soc_clint
COCOTB_RESULTS_FILE ?= results_soc_clint.xml
export COCOTB_RESULTS_FILE

# This block includes nothing, but -I$(RTL) keeps the invocation identical
# to the sibling fabric suite and to the flow scripts.
COMPILE_ARGS += -I$(RTL)
COMPILE_ARGS += $(EXTRA_COMPILE_ARGS)

include $(shell cocotb-config --makefiles)/Makefile.sim

# Default-flag Verilator lint. The three-state machine is small enough that
# any warning here is a real one.
VERILATOR ?= $(firstword $(shell command -v verilator 2>/dev/null) \
	$(wildcard $(HOME)/oss-cad-suite/bin/verilator \
	           $(HOME)/Documents/gt2n-soc/tools/oss-cad-suite/bin/verilator \
	           $(HOME)/Downloads/oss-cad-suite-linux-x64-20260804/oss-cad-suite/bin/verilator))

.PHONY: lint
lint:
	@test -n "$(VERILATOR)" || \
		{ echo "verilator not found: install oss-cad-suite or put it on PATH"; exit 1; }
	$(VERILATOR) --lint-only -I$(RTL) $(SOC_CLINT_SRC)

endif
