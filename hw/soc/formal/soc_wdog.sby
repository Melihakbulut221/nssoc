# SPDX-FileCopyrightText: 2026 Hasan Melih Akbulut
# SPDX-License-Identifier: CERN-OHL-W-2.0

# SymbiYosys job for the watchdog.
#
#   sby -f soc_wdog.sby prove    k-induction proof of W1-W5
#   sby -f soc_wdog.sby bmc      bounded check, depth 40
#   sby -f soc_wdog.sby cover    reachability of the whole escalation
#                                ladder, which is what stops a set of
#                                pure safety properties from being
#                                satisfied by a block that does nothing
#
# Properties live in soc_wdog_props.v, textually included at the end of
# the soc_wdog module body under `ifdef FORMAL (read_verilog -formal
# defines FORMAL). They are the five requirements W1-W5 of the RTL's own
# header, which docs/40-interrupts-timers-watchdog.md section 5 argues
# and which were written before the RTL.
#
# THE PARAMETERS ARE SMALL ON PURPOSE. WIDTH 8 and PRESCALE 4 make a full
# timeout 1,024 clocks instead of the million soc_top.v instantiates, and
# RST_CYCLES 4 keeps the reset pulse inside a bounded check. What is
# proved is therefore the POLICY at one configuration, not the policy at
# every configuration -- and in particular the cover task's claim that
# stage 3 is reachable is a claim about ESCALATE = 2. The behaviour is
# parameterised; the proof is at one point.
#
# W7 and W8, added by docs/43-core-hardening.md, are proved here too,
# and the `prove_w0` task proves the same file with WINDOW = 0 -- the
# configuration in which neither exists. Nothing in W1-W5 changed; the
# load-bearing new property is W7b, which says that at the reset
# configuration the two mechanisms cannot fire at all, so a program that
# never writes WDOGWIN gets the block docs/42 measured.
#
# W6, the protection added by docs/41-watchdog-hardening.md, is present
# in this job's model -- the three soc_tmr_bank replicas and the voter
# are elaborated, so every property below is proved over the VOTED state
# rather than over a plain register. That is a regression statement and
# not a redundancy proof: it says the hardening did not change the
# policy. The redundancy itself -- that the replicas round-trip their
# storage transforms, that they agree without a fault, and that a fault
# free in every bit of any one replica is masked and reported exactly --
# is soc_wdog_tmr.sby, which drives a fault model this job has no way to
# express. Neither job substitutes for the other.

[tasks]
bmc
prove
prove_w0
cover

[options]
bmc: mode bmc
bmc: depth 40
prove: mode prove
prove: depth 15
# WINDOW = 0 removes W7 and W8 entirely. Proving the same properties
# there is the regression statement in its strongest form: the file
# without the new requirements still satisfies W1-W5, and W7b holds
# vacuously, so nothing about the two configurations differs except the
# feature. There is no cover task at WINDOW = 0 because the new covers
# are unreachable by construction in it -- which is the point.
prove_w0: mode prove
prove_w0: depth 15
cover: mode cover
cover: depth 60

[engines]
smtbmc yices

[script]
read_verilog -formal soc_wdog.v
read_verilog -formal soc_tmr_bank.v
read_verilog -formal tmr_voter.v
chparam -set WIDTH 8 -set PRESCALE 4 -set RST_CYCLES 4 -set ESCALATE 2 soc_wdog
prove_w0: chparam -set WINDOW 0 soc_wdog
prep -top soc_wdog

[files]
../rtl/soc_wdog.v
../rtl/soc_tmr_bank.v
../../rtl/tmr_voter.v
soc_wdog_props.v
