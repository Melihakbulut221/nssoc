*.pyc
__pycache__/
.venv/
runs/
*.vcd
*.fst

# verification harness artifacts
hw/.venv/
hw/tb/sim_build*/
hw/tb/results.xml
hw/tb/results_*.xml
hw/soc/tb/cocotb/sim_build*/
hw/soc/tb/cocotb/results_*.xml
.pytest_cache/

# SymbiYosys working directories: one per task. The .sby configs and the
# property files are the tracked source; these are regenerated output.
formal/aer_fifo/
formal/aer_fifo_*/
formal/npu_regbank/
formal/npu_regbank_*/
formal/secded/
formal/secded_*/
formal/tmr_voter/
formal/tmr_voter_*/
formal/lif_ctrl/
formal/lif_ctrl_*/
formal/lif_mem/
formal/lif_mem_*/
formal/scrub/
formal/scrub_*/

# generated documentation site
_site/
_site_builtin/

# physical flow run outputs
hw/openlane/runs/
hw/openlane/*/runs/
tt/runs/

# tt-support-tools is cloned in for the submission checks, not vendored
tt/tt/

# Ibex bring-up (hw/soc, docs/38). Same rule as tt/tt/ above: fetched in
# for a check, not vendored. `ext/` is TWO upstream git clones -- Ibex,
# and riscv-formal since docs/63 -- `tools/` is two downloaded
# toolchains (sv2v, riscv-none-elf-gcc), and `gen/`, `out/` are
# regenerated by hw/soc/flow/. The tracked sources are
# hw/soc/{flow,rtl,tb,syn,sta,fi,rvformal}/ and hw/soc/tools.soc.mk,
# which pins every one of the fetched things by commit or sha256 so that
# what is ignored is still reproducible.
hw/soc/ext/
hw/soc/tools/
hw/soc/gen/
# hw/soc/genrvfi/ is the SAME sv2v conversion of the SAME pinned Ibex
# commit with --define=RVFI added, so the core carries the 45 RVFI trace
# ports riscv-formal binds to (docs/63). It is a SECOND tree and not a
# replacement: hw/soc/gen/ must go on reproducing byte-identically,
# because every area, timing and campaign number from docs/38 to
# docs/62 was measured from it.
hw/soc/genrvfi/
# hw/soc/genp/ is gen/ibex_top.v with the register file's fault port
# added, written by flow/ibex_fault_port.py (docs/44 section 4). Same
# rule: derived from a pinned input by a tracked script, never
# vendored and never edited by hand.
hw/soc/genp/
# hw/soc/gen_spi_host/ is the sv2v conversion of OpenTitan's spi_host
# closure at the commit hw/soc/tools.soc.mk pins, written by
# hw/soc/flow/sv2v_spi_host.sh (docs/65). Same rule as gen/: derived
# from a pinned input by a tracked script, never vendored.
hw/soc/gen_spi_host/
hw/soc/out/

# SymbiYosys working directories for the SoC fabric jobs (docs/39). Same
# rule as the formal/ entries above: the .sby configs and the property
# files are the tracked source, these are regenerated output.
hw/soc/formal/soc_bus/
hw/soc/formal/soc_bus_*/
hw/soc/formal/soc_apb_bridge/
hw/soc/formal/soc_apb_bridge_*/
hw/soc/formal/soc_clint/
hw/soc/formal/soc_clint_*/
hw/soc/formal/soc_wdog/
hw/soc/formal/soc_wdog_*/
hw/soc/formal/regfile_secded/
hw/soc/formal/regfile_secded_*/
hw/soc/formal/soc_scrub/
hw/soc/formal/soc_scrub_*/
hw/soc/formal/byte_secded/
hw/soc/formal/byte_secded_*/
hw/soc/formal/soc_mem_ecc/
hw/soc/formal/soc_mem_ecc_*/
hw/soc/formal/soc_busstat/
hw/soc/formal/soc_busstat_*/
hw/soc/formal/soc_npu_ser/
hw/soc/formal/soc_npu_ser_*/
hw/soc/formal/soc_gpio/
hw/soc/formal/soc_gpio_*/
hw/soc/formal/soc_qspi/
hw/soc/formal/soc_qspi_*/
hw/soc/formal/soc_boot/
hw/soc/formal/soc_boot_*/
# docs/77: the clock-gate enable transformation's six task directories.
hw/soc/formal/clkgate_wake/
hw/soc/formal/clkgate_wake_*/
hw/soc/tb/cocotb/mutants_soc_qspi/
# docs/68: the boot register block's mutation harness builds and its
# per-mutant result files, and the loader's own build products.
hw/soc/tb/cocotb/sim_build_mutb_*/
hw/soc/tb/cocotb/results_mutb_*.xml
hw/soc/pnr/state/

# docs/47: the place-and-route flow. Generated and fetched, never
# vendored -- the rule docs/38 section 11 states. The state file names
# an absolute path to a netlist and is written by flow/pnr_soc_top.sh.
hw/soc/pnr/runs/
hw/soc/pnr/config.resolved.json
hw/soc/pnr/state/

# docs/48: the floorplan variants. hw/soc/pnr/floorplan.py writes them
# from the PDK's own LEF and from config.json, and reproduces docs/47's
# floorplan exactly at --channel 700.08 --density 40, so committing one
# would be vendoring a thing the tree can regenerate.
hw/soc/pnr/config-fp*.json

# docs/73: config-ecc.json plus MANUAL_GLOBAL_PLACEMENTS, written by
# hw/soc/pnr/clint_region.py config from the tracked clint_placement.json.
hw/soc/pnr/config-ecc-clint*.json

# scripts/run_cocotb.sh single-run lock
.run_cocotb.lock

# formal/eqy build output
formal/eqy/out/

# scripts/ci_local.sh build output
_site/
_site_builtin/

# paper/render_html.py output
paper/main.html

# pdflatex output. Deliberately NOT tracked, and the reason WAS the same
# one docs/80 section 4 gives about digests: there is no pdflatex on the
# development machine, so a committed PDF is a binary this repository
# cannot rebuild, cannot diff and cannot check against paper/main.tex.
# The published mirror's CI compiles it on every push and uploads it as
# a build artefact, which is where a downloadable copy comes from.
#
# BOTH HALVES OF THAT REASON FAILED, and it is worth writing down which
# way. 2026-09-14: tectonic 0.17.0 is now installed and `make -C paper`
# builds the PDF here, so "cannot rebuild" is no longer true. And CI has
# not started a job since 2026-09-10 -- the runs fail with zero steps
# executed, which is the account's Actions spending limit -- so "CI
# uploads it as a build artefact" has not been true either, and the
# consequence is that for four days there was NO downloadable copy of
# this paper anywhere.
#
# The file stays untracked: a binary in git is still not diffable, and
# that part of the argument never depended on the missing toolchain. The
# downloadable copy is a RELEASE ASSET on the mirror instead, built from
# a named revision, which is the form that says which source it came
# from.
paper/main.pdf
paper/*.aux
paper/*.bbl
paper/*.blg
paper/*.log
paper/*.out
# `make -C paper arxiv` output. Untracked for the same reason as the PDF
# and for one more: it is a tarball of two files that are both already
# in this repository, so publishing it would put main.tex in the tree
# twice, once where it can be diffed and once where it cannot. It was
# committed by accident on 2026-09-14 -- the first `make arxiv` this
# repository has ever been able to run produced it, and nothing was
# ignoring a file that had never existed before. Caught by reading the
# mirror's staged file list rather than by any check, which is the
# reason this line exists instead of a note saying it was fine.
paper/arxiv.tar.gz
paper/*.out
