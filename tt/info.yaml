# Tiny Tapeout project information.
#
# GENERATED by scripts/gen_tt_submission.py in the
# neuromorphic-space-soc repository. Do not edit here.
#
# Schema: tt-support-tools project_info.py, yaml_version 6
# (01d5d2814fa9).
project:
  title:        "NSSOC-P1 fault-tolerant spiking neuron pilot"
  author:       "Hasan Melih Akbulut"
  discord:      ""
  description:  "8x8 leaky integrate-and-fire neuron tile with a TMR-voted configuration domain, a SECDED-protected weight path, AER event queues and on-chip fault counters"
  language:     "Verilog"

  # 50 MHz. A local place-and-route of this design at this tile
  # shape closes with +1.23 ns of setup slack and +0.60 ns of
  # hold slack on the slow corner (sg13g2_stdcell 1.08 V, 125 C),
  # with a clock tree and extracted parasitics: zero setup, hold,
  # max-cap and max-slew violations on all three PVT corners.
  # Not a signoff; see docs/23 section 3 in the source
  # repository.
  clock_hz:     50000000

  # Tile shape. docs/23: placed, this design is 185,840 um2 of
  # sg13g2 standard cells, which no longer fits the 70 % planning
  # criterion in a 4x2 core of 259,837 um2 (docs/22 measures
  # 71.489 %). 6x2 offers 392,988 um2 and a full local harden
  # there closes at 47.29 % utilization with zero detailed-route
  # DRC errors, zero Magic and KLayout DRC, zero LVS errors and
  # zero antenna violations. The alternative twelve-tile shape,
  # 3x4, was hardened too and is the runner-up on every count
  # except utilization; docs/23 has both side by side.
  tiles: "6x2"

  top_module:  "tt_um_melihakbulut_nssoc"

  source_files:
    - "tt_um_melihakbulut_nssoc.v"
    - "pilot_top.v"
    - "lif_core.v"
    - "aer_fifo.v"
    - "tmr_voter.v"
    - "secded_enc.v"
    - "secded_dec.v"

# Pin contract. Identical to hw/rtl/pilot_top.v section 1 and to
# docs/15 section 2 in the source repository.
pinout:
  # Inputs
  ui[0]: "SER_SCK"
  ui[1]: "SER_CS_N"
  ui[2]: "SER_MOSI"
  ui[3]: "AER_IN_STB"
  ui[4]: "AER_IN_TICK"
  ui[5]: "AER_OUT_ACK"
  ui[6]: "SCRUB_STB"
  ui[7]: ""

  # Outputs
  uo[0]: "SER_MISO"
  uo[1]: "BUSY"
  uo[2]: "AER_IN_RDY"
  uo[3]: "AER_OUT_VLD"
  uo[4]: "ERR"
  uo[5]: "SEC"
  uo[6]: "DED"
  uo[7]: "TMR"

  # Bidirectional pins: uio_oe is the constant 0xF0, so uio[3:0]
  # are always inputs and uio[7:4] are always outputs.
  uio[0]: "AER_IN_ADDR[0]"
  uio[1]: "AER_IN_ADDR[1]"
  uio[2]: "AER_IN_ADDR[2]"
  uio[3]: "AER_IN_ADDR[3]"
  uio[4]: "AER_OUT_ID[0]"
  uio[5]: "AER_OUT_ID[1]"
  uio[6]: "AER_OUT_ID[2]"
  uio[7]: "AER_OUT_ID[3]"

# Do not change!
yaml_version: 6
